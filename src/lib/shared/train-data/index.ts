/** The classes that the model is trained to recognize. */
export type Shape = 'Circle' | 'Rectangle';

/** The stroke color of the shapes that the model is trained on. */
export const strokeColor = '#000';

/** The background color of the shape drawings that the model is trained on. */
export const backgroundColor = '#fff';

/** The size of a square input image that the model was trained on (in pixels). */
export const inputImageSize = 416;
